# orderbook_dex
A decentralized exchange using the orderbook model for matching buy and sell orders. On-chain orderbook with limit and market orders where users can swap from eth to some token and from the token back to eth.
